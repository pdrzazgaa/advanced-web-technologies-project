import { Box, Stack } from "@mui/material";
import React, { FC } from "react";

const SearchResults: FC = () => {
  return (
    <Box sx={{ backgroundColor: "background.default" }}>
      <Stack sx={{ color: "text.secondary" }}>results</Stack>
    </Box>
  );
};

export default SearchResults;
